import React from 'react';
    import Button from '@mui/material/Button';

    function App() {
      return (
        <div>
          <h1>Welcome to the Automated File Organization Tool</h1>
          <Button variant="contained" onClick={() => alert('Button clicked!')}>
            Click Me
          </Button>
        </div>
      );
    }

    export default App;
